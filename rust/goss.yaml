file:
  # ensure a basic shell exists
  /bin/sh:
    exists: true

  # ensure basic directory structure created
  /home/circleci:
    exists: true

package:
  musl:
    installed: true
  musl-dev:
    installed: true
  musl-tools:
    installed: true

user:
  # ensure circleci user exists
  circleci:
    exists: true
